# P3-X03: Updated Cost Model

You are working in /repo/scripts/cost-model/. Update the cost model with measured parameters.

## Context

After pilot deployment and stress testing, we have measured values for parameters that were estimated in P0-X01. Update the cost model.

## Reading Instructions

Before writing any code, read:
- .agents/handoff/P0-X01.md — original cost model, params.yaml
- .agents/handoff/P2-E03.md — stress test results (once available)
- .agents/handoff/P2-O03.md — measured calibration pass-through rates
- scripts/cost-model/ — existing cost model scripts
- CONVENTIONS.md

## Implementation

1. Update scripts/cost-model/params.yaml:
   - Replace all "REPLACE WITH MEASURED" values with actuals from pilot
   - Add new parameters discovered during deployment (FAISS memory, checkpoint storage, shadow inference overhead)
2. Update scripts/cost-model/cost_model.py:
   - Add MTMC infrastructure costs (FAISS memory, Re-ID GPU)
   - Add storage tiering costs (hot/warm/cold)
   - Add annotation costs (continuous pipeline)
   - Add shadow deployment overhead
3. Generate updated projections for 10, 50, 100 camera scenarios
4. Create docs/cost-model-v2.md — updated cost analysis with measured vs estimated comparison

## Constraints

- Do NOT remove the original estimates — keep them for comparison
- Must generate Excel output
- Must pass `ruff check`

## Expected Deliverables

- scripts/cost-model/params-measured.yaml
- scripts/cost-model/cost_model_v2.py
- docs/cost-model-v2.md
- .agents/handoff/P3-X03.md
