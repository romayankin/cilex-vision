# P1-V07: Debug Trace Pipeline

You are working in /repo/services/inference-worker/ (extending P1-V04).

Enhance debug_trace.py:
- TraceCollector class: configurable sample_rate (default 0.02), always_trace if confidence < 0.3 or manual_flag=true.
- Collect per trace: detector input frame URI, raw detector output (all boxes pre-NMS as JSON), tracker state delta, attribute outputs, model_versions, Kafka offset, camera_id + timestamps.
- Store as JSON + frame ref in MinIO bucket 'debug-traces'. Key: {camera_id}/{date}/{trace_id}.json.
- MinIO lifecycle: delete after 30 days.

Add to query-api (routers/debug.py):
- GET /debug/traces?camera_id=&start=&end=&track_id= → list with MinIO signed URLs.
- GET /debug/traces/{trace_id} → full JSON.
- Restrict to engineering role.

Tests: verify sampling rate, low-confidence always traced, JSON valid, MinIO storage works.

## Expected Deliverables

- `services/inference-worker/debug_trace.py`
- `services/query-api/routers/debug.py`
