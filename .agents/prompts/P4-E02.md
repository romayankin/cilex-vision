# P4-E02: Zone Benchmark

You are working in /repo/scripts/evaluation/. Validate MTMC zone sharding performance.

## Context

After implementing zone sharding (P4-V01), we need to benchmark that cross-zone matching quality is acceptable and that zone sharding actually improves performance.

## Reading Instructions

Before writing any code, read:
- .agents/handoff/P4-V01.md — zone sharding implementation (once available)
- .agents/handoff/P2-E02.md — MTMC evaluation methodology
- .agents/handoff/P2-V02.md — baseline MTMC performance
- CONVENTIONS.md

## Implementation

1. Create scripts/evaluation/zone_benchmark.py:
   - Compare: full-site MTMC vs zone-sharded MTMC on same dataset
   - Metrics: match accuracy (rank-1, mAP), false positive rate, latency, memory usage
   - Vary zone sizes: 10, 25, 50 cameras per zone
   - Cross-zone match quality: accuracy at zone boundaries
2. Create scripts/evaluation/generate_zone_dataset.py:
   - Generate synthetic multi-zone dataset with known identities
   - Include cross-zone identities at boundary cameras
3. Produce benchmark report with scaling curves

## Constraints

- Must compare against baseline (non-sharded) MTMC
- Log all results to MLflow experiment 'zone-benchmark'
- Must pass `ruff check`

## Expected Deliverables

- scripts/evaluation/zone_benchmark.py
- scripts/evaluation/generate_zone_dataset.py
- docs/evaluation-results/zone-benchmark-report.md
- .agents/handoff/P4-E02.md
