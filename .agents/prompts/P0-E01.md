# P0-E01: Model Bake-Off Protocol

You are working in /repo. Replace the stub at docs/bake-off-protocol.md. Also create:

1. docs/bake-off-protocol.md (replace stub) — formal protocol:
   - DETECTOR: 10 days, candidates (YOLOv8-L, YOLOv9-C, RT-DETR-L), metrics (mAP@0.5, mAP@0.5:0.95, per-class AP, latency, throughput, small_object_AP, night_AP), decision formula: score = 0.35*mAP + 0.25*throughput + 0.2*small_obj + 0.2*night_perf.
   - TRACKER: 5 days, ByteTrack vs BoT-SORT, MOTA/IDF1/ID_switches/fragmentation.
   - ATTRIBUTE: 3 days, 2 color classifiers, per-color accuracy + confusion matrix.

2. scripts/bakeoff/run_detector_bakeoff.py — harness: ONNX→TensorRT→Triton→eval dataset→MLflow logging.
3. scripts/bakeoff/compare_bakeoff.py — read MLflow runs, produce Markdown comparison + matplotlib charts.
4. Time-box: if no clear winner, pick safest default.

## Expected Deliverables

- docs/bake-off-protocol.md
- scripts/bakeoff/run_detector_bakeoff.py
- scripts/bakeoff/compare_bakeoff.py
- .agents/handoff/P0-E01.md
