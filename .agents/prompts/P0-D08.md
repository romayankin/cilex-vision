# P0-D08: Security Design — mTLS & ACLs

You are working in /repo. Replace the stub at docs/security-design.md. Also create:

1. docs/security-design.md (replace stub) — trust model (cameras untrusted, edge→NATS mTLS, NATS→Kafka TLS+SASL, signed MinIO URLs, camera VLAN isolation), PKI plan (step-ca, per-site certs, 90-day rotation), NATS auth (mTLS, per-site subject ACLs), Kafka auth (TLS, SCRAM-SHA-256, topic ACLs), network isolation diagram.
2. infra/pki/step-ca-config.json — step-ca provisioner config.
3. infra/pki/bootstrap-site.sh — generate and deploy certs for new edge site.
4. infra/nats/nats-server.conf — NATS config with TLS and authorization blocks.
5. infra/kafka/server.properties.template — Kafka broker TLS/SASL config.

## Expected Deliverables

- `docs/security-design.md`
- `infra/pki/step-ca-config.json`
- `infra/pki/bootstrap-site.sh`
- `infra/nats/nats-server.conf`
- `infra/kafka/server.properties.template`
