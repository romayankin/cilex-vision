# P1-E01: Detector Bake-Off Execution (10 days)

You are working in /repo/scripts/bakeoff/. Execute the protocol from docs/bake-off-protocol.md.

For each of 3 candidates (YOLOv8-L, YOLOv9-C, RT-DETR-L):
1. Export to ONNX
2. Convert to TensorRT FP16
3. Integrate into Triton model repo
4. Run on evaluation dataset from data/eval/
5. Log to MLflow: mAP@0.5, mAP@0.5:0.95, per-class AP, latency_ms, throughput_fps, small_object_AP, night_AP

Then: compare_bakeoff.py → Markdown comparison table + charts + RECOMMENDATION.
