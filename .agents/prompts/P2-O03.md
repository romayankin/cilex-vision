# P2-O03: Calibration Scheduler

You are working in /repo/scripts/calibration/. Automate the edge filter calibration rotation from P1-E03.

## Context

P1-E03 created the calibration measurement script. This task automates it as a scheduled rotation across all cameras.

## Reading Instructions

Before writing any code, read:
- .agents/handoff/P1-E03.md — edge_filter_calibration.py, calibration protocol
- .agents/handoff/P1-V01.md — edge agent control subjects (NATS)
- scripts/calibration/ — existing calibration scripts
- CONVENTIONS.md

## Implementation

1. Create scripts/calibration/calibration_scheduler.py:
   - Reads camera list from DB
   - Rotates through all cameras, one at a time
   - Runs calibration measurement per P1-E03 protocol
   - Configurable interval (default: weekly per camera)
   - Stores results in DB table: calibration_results (camera_id, timestamp, pass_through_rate, miss_rate, false_trigger_rate)
2. Create infra/cron/calibration-cron.yml — systemd timer or cron entry
3. Create scripts/calibration/calibration_report.py — generate per-camera calibration trend report
4. Auto-update cost model params.yaml with latest measured pass-through rates

## Constraints

- Only one camera calibrates at a time (avoid overloading central inference)
- Do NOT modify edge agent code
- Must pass `ruff check`

## Expected Deliverables

- scripts/calibration/calibration_scheduler.py
- scripts/calibration/calibration_report.py
- infra/cron/calibration-cron.yml
- .agents/handoff/P2-O03.md
