# P0-D04: Database Schema (TimescaleDB + PostgreSQL)

You are working in /repo/services/db/. Replace the stub models.py with real SQLAlchemy models.

TIMESCALEDB hypertables (high-volume, append-only):
- detections: time (TIMESTAMPTZ, partition key), camera_id, frame_seq, class, confidence, bbox_x/y/w/h, local_track_id, model_version. Chunk 1h. Retention 30d. Compress after 2d.
- track_observations: time, camera_id, local_track_id, centroid_x/y, bbox_area, embedding_ref. Chunk 1h. Retention 30d.

POSTGRESQL relational tables:
- cameras, sites, topology_edges, local_tracks, global_tracks, global_track_links
- track_attributes, events (with metadata_jsonb), users, audit_logs

Create:
- services/db/models.py — SQLAlchemy async models
- services/db/alembic.ini + alembic/env.py + initial migration
- Migration: create all tables, convert to hypertables, add retention/compression policies, create indexes
- docs/diagrams/schema.mermaid — ER diagram

## Expected Deliverables

- `services/db/models.py`
- `services/db/alembic.ini`
- `services/db/alembic/env.py`
- `docs/diagrams/schema.mermaid`
