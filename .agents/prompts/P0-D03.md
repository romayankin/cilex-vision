# P0-D03: Kafka Topic Contract & Partitioning Plan

You are working in /repo. Replace the stub at docs/kafka-contract.md with the full contract. Also create:

1. **infra/kafka/topics.yaml** — YAML listing every topic:
   - frames.sampled.refs: key=camera_id, 12 partitions, delete, 2h retention
   - tracklets.local: key=camera_id, 12 partitions, delete, 6h
   - attributes.jobs: key=local_track_id, 6 partitions, delete, 2h
   - mtmc.active_embeddings: key=local_track_id, 12 partitions, compact, infinite
   - events.raw: key=event_id, 6 partitions, delete, 7d
   - archive.transcode.requested: key=camera_id, 4 partitions, delete, 24h
   - archive.transcode.completed: key=camera_id, 4 partitions, delete, 24h
   Each entry: name, partitions, replication_factor, cleanup_policy, retention_ms, key_schema, value_schema, description.

2. **infra/kafka/create-topics.py** — Python script reading topics.yaml, creating topics via confluent_kafka AdminClient. Idempotent. Accept --bootstrap-servers arg.

3. **docs/kafka-contract.md** — Replace stub. For each topic: what produces, what consumes, message format, ordering guarantees, why that key was chosen.

CRITICAL: No image/video bytes on Kafka — only URI references.
