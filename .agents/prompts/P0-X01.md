# P0-X01: Parametric Cost Model

You are working in /repo/scripts/cost-model/. Create:

1. params.yaml — all input parameters with defaults:
   cameras, bitrate_mbps, motion_duty_cycle, inference_fps, detections_per_frame,
   active_tracks_per_camera, avg_event_clip_seconds, retention periods,
   gpu_cost_monthly, cameras_per_gpu, storage costs by tier, kafka/db costs.

2. cost_model.py — reads params.yaml, computes monthly cost:
   - Raw storage/day, effective central load, GPU nodes needed
   - Event clips/day, warm storage with re-encode ratio
   - Three scenarios: P25/P50/P90 motion duty cycle
   - Output: formatted table for 4/10/100 cameras
   - Also generate Excel via openpyxl

Mark every assumed parameter with "REPLACE WITH MEASURED".

## Expected Deliverables

- `scripts/cost-model/params.yaml`
- `scripts/cost-model/cost_model.py`
