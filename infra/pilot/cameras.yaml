# Pilot camera configuration — loaded by the edge-agent.
# Edit the rtsp_url values to match your camera network.

site_id: pilot-site

cameras:
  - camera_id: cam-1
    rtsp_url: "rtsp://admin:Qwerty12@192.168.1.66:554/Streaming/Channels/101"
    enabled: true
